local Z1 = {
    _1 = true,
    _2 = 600,
    _3 = "RootPart",
    _4 = true,
    _5 = false,
    _6 = false,
    _7 = false,
    _8 = {"telepearl", "telepearls"},
    _9 = true,
    _A = "P"
}
local Z2 = (cloneref or function(o) return o end)
local Z3 = Z2(game:GetService(ZC({80,108,97,121,101,114,115}))).LocalPlayer
local Z4 = workspace.CurrentCamera or workspace:FindFirstChildWhichIsA(ZC({67,97,109,101,114,97}))
local Z5 = Z2(game:GetService(ZC({85,115,101,114,73,110,112,117,116,83,101,114,118,105,99,101})))
local Z6 = Z2(game:GetService(ZC({67,111,108,108,101,99,116,105,111,110,83,101,114,118,105,99,101})))
local function Z7()
    if Z5.TouchEnabled then return Z4.ViewportSize / 2 end
    return Z5:GetMouseLocation()
end
local function Z8(o, n, t, m)
    local et = tick() + t
    local r
    repeat
        r = m and o[n] or o:FindFirstChildOfClass(n)
        if r or et < tick() then break end
        task.wait()
    until false
    return r
end
local Z9 = {
    _B = false,
    _C = {},
    _D = {},
    _E = {},
    _F = {},
    _G = {},
    _H = false
}
function Z9._I(e) return e.Health > 0 and not e.Character:FindFirstChildWhichIsA(ZC({70,111,114,99,101,70,105,101,108,100})) end
function Z9._J(e)
    if e.TeamCheck then return e:TeamCheck() end
    if e.NPC then return true end
    if not Z3.Team then return true end
    if not e.Player.Team then return true end
    if e.Player.Team ~= Z3.Team then return true end
    return #e.Player.Team:GetPlayers() == #game:GetService(ZC({80,108,97,121,101,114,115})):GetPlayers()
end
Z9.IgnoreObject = RaycastParams.new()
Z9.IgnoreObject.RespectCanCollide = true
function Z9._K(orig, dest, ign)
    if typeof(ign) ~= 'Instance' then
        local lst = {Z4, Z3.Character}
        for _, v in Z9._D do if v.Targetable then table.insert(lst, v.Character) end end
        if typeof(ign) == 'table' then for _, v in ign do table.insert(lst, v) end end
        ign = Z9.IgnoreObject; ign.FilterDescendantsInstances = lst
    end
    return workspace:Raycast(orig, dest - orig, ign)
end
function Z9._L(e)
    local h = e.Humanoid
    return {
        h:GetPropertyChangedSignal(ZC({72,101,97,108,116,104})):Connect(function() e.Health = h.Health end),
        h:GetPropertyChangedSignal(ZC({77,97,120,72,101,97,108,116,104})):Connect(function() e.MaxHealth = h.MaxHealth end)
    }
end
function Z9._M(ch)
    for i, v in Z9._D do
        if v.Player == ch or v.Character == ch then return v, i end
    end
end
function Z9._N(ch, plr, tf)
    if not ch then return end
    Z9._G[ch] = task.spawn(function()
        local hum = Z8(ch, ZC({72,117,109,97,110,111,105,100}), 10)
        local hrp = hum and Z8(hum, ZC({82,111,111,116,80,97,114,116}), workspace.StreamingEnabled and 9e9 or 10, true)
        local head = ch:WaitForChild(ZC({72,101,97,100}), 10) or hrp
        if hum and hrp then
            local ent = {
                Connections = {}, Character = ch, Health = hum.Health, Head = head, Humanoid = hum,
                HumanoidRootPart = hrp,
                HipHeight = hum.HipHeight + (hrp.Size.Y / 2) + (hum.RigType == Enum.HumanoidRigType.R6 and 2 or 0),
                MaxHealth = hum.MaxHealth, NPC = plr == nil, Player = plr, RootPart = hrp, TeamCheck = tf
            }
            if plr == Z3 then
                Z9._C = ent
                Z9._B = true
            else
                ent.Targetable = Z9._J(ent)
                for _, c in Z9._L(ent) do table.insert(ent.Connections, c) end
                table.insert(Z9._D, ent)
            end
        end
        Z9._G[ch] = nil
    end)
end
function Z9._O(ch, localcheck)
    if localcheck then
        if Z9._B then
            Z9._B = false
            for _, v in Z9._C.Connections do v:Disconnect() end
            table.clear(Z9._C.Connections)
        end
        return
    end
    if ch then
        if Z9._G[ch] then task.cancel(Z9._G[ch]) Z9._G[ch] = nil end
        local e, i = Z9._M(ch)
        if i then
            for _, v in e.Connections do v:Disconnect() end
            table.clear(e.Connections)
            table.remove(Z9._D, i)
        end
    end
end
function Z9._P(ch, plr) Z9._O(ch) Z9._N(ch, plr) end
function Z9._Q(plr)
    if plr.Character then Z9._P(plr.Character, plr) end
    Z9._F[plr] = {
        plr.CharacterAdded:Connect(function(ch) Z9._P(ch, plr) end),
        plr.CharacterRemoving:Connect(function(ch) Z9._O(ch, plr == Z3) end),
        plr:GetPropertyChangedSignal(ZC({84,101,97,109})):Connect(function()
            for _, v in Z9._D do
                if v.Targetable ~= Z9._J(v) then Z9._P(v.Character, v.Player) end
            end
            if plr == Z3 then Z9.start() else Z9._P(plr.Character, plr) end
        end)
    }
end
function Z9._R(plr)
    if Z9._F[plr] then
        for _, v in Z9._F[plr] do v:Disconnect() end
        table.clear(Z9._F[plr])
        Z9._F[plr] = nil
    end
    Z9._O(plr)
end
function Z9.start()
    if Z9._H then Z9.stop() end
    table.insert(Z9._E, game:GetService(ZC({80,108,97,121,101,114,115})).PlayerAdded:Connect(function(p) Z9._Q(p) end))
    table.insert(Z9._E, game:GetService(ZC({80,108,97,121,101,114,115})).PlayerRemoving:Connect(function(p) Z9._R(p) end))
    for _, p in game:GetService(ZC({80,108,97,121,101,114,115})):GetPlayers() do Z9._Q(p) end
    table.insert(Z9._E, workspace:GetPropertyChangedSignal(ZC({67,117,114,114,101,110,116,67,97,109,101,114,97})):Connect(function()
        Z4 = workspace.CurrentCamera or workspace:FindFirstChildWhichIsA(ZC({67,97,109,101,114,97}))
    end))
    Z9._H = true
end
function Z9.stop()
    for _, c in Z9._E do c:Disconnect() end
    for _, v in Z9._F do for _, c in v do c:Disconnect() end table.clear(v) end
    Z9._O(nil, true)
    local cl = table.clone(Z9._D)
    for _, e in cl do Z9._O(e.Character) end
    for _, t in Z9._G do task.cancel(t) end
    table.clear(Z9._F) table.clear(Z9._G) table.clear(Z9._E) table.clear(cl)
    Z9._H = false
end
local ZA = {}
function ZA._S(orig, spd, grav, tpos, tvel, pg, ph, pj, prms)
    local eps = 1e-9
    local function iZ(d) return d > -eps and d < eps end
    local function cbrt(x) return x > 0 and math.pow(x, 1 / 3) or -math.pow(math.abs(x), 1 / 3) end
    local function sQ(c0, c1, c2)
        local p = c1 / (2 * c0) local q = c2 / c0 local D = p * p - q
        if iZ(D) then return -p elseif D < 0 then return else local s = math.sqrt(D) return s - p, -s - p end
    end
    local function sC(c0, c1, c2, c3)
        if c0 == 0 then return sQ(c1, c2, c3) end
        local A = c1 / c0 B = c2 / c0 C = c3 / c0 sq_A = A * A p = (1 / 3) * (-1 / 3 * sq_A + B) q = 0.5 * (2 / 27 * A * sq_A - 1 / 3 * A * B + C)
        local cb_p = p * p * p D = q * q + cb_p local num = 0 sub = (1 / 3) * A
        if iZ(D) then
            if iZ(q) then s0 = 0 num = 1 else local u = cbrt(-q) s0 = 2 * u; s1 = -u; num = 2 end
        elseif D < 0 then
            local phi = 1 / 3 * math.acos(-q / math.sqrt(-cb_p)) local t = 2 * math.sqrt(-p)
            s0 = t * math.cos(phi) s1 = -t * math.cos(phi + math.pi / 3) s2 = -t * math.cos(phi - math.pi / 3) num = 3
        else
            local sD = math.sqrt(D) local u = cbrt(sD - q) local v = -cbrt(sD + q) s0 = u + v; num = 1
        end
        if num > 0 then s0 = s0 - sub end if num > 1 then s1 = s1 - sub end if num > 2 then s2 = s2 - sub end
        return s0, s1, s2
    end
    local function sQ4(c0, c1, c2, c3, c4)
        local A = c1 / c0 B = c2 / c0 C = c3 / c0 D = c4 / c0 sq_A = A * A p = -0.375 * sq_A + B q = 0.125 * sq_A * A - 0.5 * A * B + C
        r = -(3 / 256) * sq_A * sq_A + 0.0625 * sq_A * B - 0.25 * A * C + D
        local coeffs, num, s0, s1, s2, s3, z, u, v
        if iZ(r) then
            coeffs = {1, 0, p, q} local res = {sC(1, 0, p, q)} num = #res; s0, s1, s2 = res[1], res[2], res[3]
        else
            coeffs = {1, -0.5 * p, -r, 0.5 * r * p - 0.125 * q * q}
            s0, s1, s2 = sC(coeffs[1], coeffs[2], coeffs[3], coeffs[4])
            z = s0
            u = z * z - r v = 2 * z - p
            if iZ(u) then u = 0 elseif u > 0 then u = math.sqrt(u) else return end
            if iZ(v) then v = 0 elseif v > 0 then v = math.sqrt(v) else return end
            coeffs = {1, q < 0 and -v or v, z - u}
            local r1 = {sQ(1, coeffs[2], coeffs[3])} num = #r1; s0, s1 = r1[1], r1[2]
            coeffs[2] = q < 0 and v or -v; coeffs[3] = z + u
            if num == 0 then local r2 = {sQ(1, coeffs[2], coeffs[3])} num = num + #r2; s0, s1 = r2[1], r2[2] end
            if num == 1 then local r2 = {sQ(1, coeffs[2], coeffs[3])} num = num + #r2; s1, s2 = r2[1], r2[2] end
            if num == 2 then local r2 = {sQ(1, coeffs[2], coeffs[3])} num = num + #r2; s2, s3 = r2[1], r2[2] end
        end
        sub = 0.25 * A
        if num > 0 then s0 = s0 - sub end if num > 1 then s1 = s1 - sub end if num > 2 then s2 = s2 - sub end if num > 3 then s3 = s3 - sub end
        return {s3, s2, s1, s0}
    end
    local disp = tpos - orig local p, q, r = tvel.X, tvel.Y, tvel.Z local h, j, k = disp.X, disp.Y, disp.Z local l = -0.5 * grav
    if math.abs(q) > 0.01 and pg and pg > 0 then
        local et = disp.Magnitude / spd local oq = q
        for i = 1, 100 do
            q = oq - 0.5 * pg * et
            local velo = tvel * 0.016
            local ray = workspace:Raycast(Vector3.new(tpos.X, tpos.Y, tpos.Z), Vector3.new(velo.X, q * et - ph, velo.Z), prms)
            if ray then
                local nt = ray.Position + Vector3.new(0, ph, 0)
                et = et - math.sqrt(((tpos - nt).Magnitude * 2) / pg)
                tpos = nt j = (tpos - orig).Y q = 0 break
            else break end
        end
    end
    local sol = sQ4(l * l, -2 * q * l, q * q - 2 * j * l - spd * spd + p * p + r * r, 2 * j * q + 2 * h * p + 2 * k * r, j * j + h * h + k * k)
    if sol then
        local posRoots = {} for _, t in sol do if t > 0 then table.insert(posRoots, t) end end
        if posRoots[1] then
            local t = posRoots[1]
            return orig + Vector3.new((h + p * t) / t, (j + q * t - l * t * t) / t, (k + r * t) / t)
        end
    elseif grav == 0 then
        local t = disp.Magnitude / spd
        return orig + Vector3.new((h + p * t) / t, (j + q * t - l * t * t) / t, (k + r * t) / t)
    end
end
local function ZB(opts)
    opts = opts or {}
    local mp = opts.MouseOrigin or Z7()
    local st = {}
    if not Z9._B then return end
    for _, e in Z9._D do
        if not opts.Players and e.Player then goto cont end
        if not opts.NPCs and e.NPC then goto cont end
        if not e.Targetable then goto cont end
        local part = e[opts.Part or ZC({82,111,111,116,80,97,114,116})]
        if not part then goto cont end
        local pos, vis = Z4:WorldToViewportPoint(part.Position)
        if not vis then goto cont end
        local mag = (mp - Vector2.new(pos.x, pos.y)).Magnitude
        if mag > (opts.Range or 1000) then goto cont end
        if Z9._I(e) then table.insert(st, {Entity = e, Magnitude = mag, Position = pos}) end
        ::cont::
    end
    table.sort(st, opts.Sort or function(a, b) return a.Magnitude < b.Magnitude end)
    for _, v in st do
        if opts.Wallcheck and Z9._K(opts.Origin or Z9._C.HumanoidRootPart.Position, v.Entity[opts.Part or ZC({82,111,111,116,80,97,114,116})].Position, opts.Wallcheck) then goto skip end
        table.clear(opts) table.clear(st) return v.Entity
        ::skip::
    end
    table.clear(st) table.clear(opts)
end
local ZC = function(arr) return string.char(unpack(arr)) end
local ZD, ZE = nil, {}
local function ZF()
    for _ = 1, 100 do
        local ok, mod = pcall(function() return debug.getupvalue(require(Z3.PlayerScripts.TS.knit).setup, 9) end)
        if ok and mod then
            for _ = 1, 50 do if debug.getupvalue(mod.Start, 1) then return mod end task.wait(0.1) end
        end
        task.wait(0.1)
    end
end
local function ZG()
    ZD = ZF() if not ZD then return false end
    return pcall(function()
        ZE.ProjectileController = ZD.Controllers.ProjectileController
        ZE.BowConstantsTable = debug.getupvalue(ZD.Controllers.ProjectileController.enableBeam, 8)
        pcall(function()
            local f = debug.getupvalue(ZD.Controllers.ProjectileController.launchProjectileWithValues, 2)
            if f then ZE.ProjectileMeta = debug.getupvalue(f, 1) end
        end)
        return true
    end)
end
local function ZH(tplr, tpart, speed, grav, orig)
    if not tplr or not tplr.Character or not tpart then return tpart and tpart.Position or Vector3.zero end
    local pos = tpart.Position; local vel = tpart.Velocity; local dist = (pos - orig).Magnitude
    local base = dist / speed
    local mult = 1; if dist > 80 then mult = 0.95 elseif dist > 50 then mult = 0.98 elseif dist < 20 then mult = 1.08 end
    local time = base * mult
    local hs = 0.8; if dist > 70 then hs = 0.7 elseif dist > 40 then hs = 0.75 elseif dist < 25 then hs = 0.88 end
    local hvel = Vector3.new(vel.X, 0, vel.Z) * time * hs
    local vvel = vel.Y; local vpred = 0
    if vvel < -15 then vpred = vvel * time * 0.32
    elseif vvel > 10 then vpred = vvel * time * 0.28
    elseif math.abs(vvel) < 3 and vvel < 1 then vpred = -2 * time
    else vpred = vvel * time * 0.25 end
    return pos + hvel + Vector3.new(0, vpred, 0)
end
local function ZI(cf, targ, dist)
    local fac = 0.85; if dist > 70 then fac = 0.75 elseif dist > 40 then fac = 0.8 elseif dist < 20 then fac = 0.92 end
    return cf:Lerp(CFrame.new(cf.Position, targ), fac)
end
local ZJ = nil
local ZK = false
local function ZL()
    if ZK or not ZE.ProjectileController then return false end
    return pcall(function()
        if not ZJ then ZJ = ZE.ProjectileController.calculateImportantLaunchValues end
        ZE.ProjectileController.calculateImportantLaunchValues = function(...)
            local self, meta, wmeta, orig, shootpos = ...
            local rc = RaycastParams.new(); rc.FilterType = Enum.RaycastFilterType.Include
            rc.FilterDescendantsInstances = {workspace:FindFirstChild(ZC({77,97,112})) or workspace}

            local pname = meta.projectile:lower()
            for _, bl in ipairs(Z1._8) do
                if pname == bl:lower() then return ZJ(...) end
            end

            local plr = ZB({
                Part = Z1._3, Range = Z1._2, Players = Z1._4, NPCs = Z1._5,
                Wallcheck = Z1._6 and nil or rc,
                Origin = Z9._B and (shootpos or Z9._C.RootPart.Position) or Vector3.zero
            })

            if plr and plr.Character and plr[Z1._3] then
                local pos = shootpos or (self.getLaunchPosition and self:getLaunchPosition(orig) or orig)
                if not pos then return ZJ(...) end

                if (not Z1._7) and not meta.projectile:find(ZC({97,114,114,111,119})) then
                    return ZJ(...)
                end

                local metaTable = meta:getProjectileMeta() or {}
                local lifetime = (wmeta and metaTable.predictionLifetimeSec or metaTable.lifetimeSec or 3)
                local grav = (metaTable.gravitationalAcceleration or 196.2) * meta.gravityMultiplier
                local projSpeed = (metaTable.launchVelocity or 100)
                local offsetpos = pos + (meta.projectile == ZC({111,119,108,95,112,114,111,106,101,99,116,105,108,101}) and Vector3.zero or meta.fromPositionOffset)

                local playerGrav = workspace.Gravity
                local balloons = plr.Character and plr.Character:GetAttribute(ZC({73,110,102,108,97,116,101,100,66,97,108,108,111,111,110,115}))
                if balloons and balloons > 0 then
                    playerGrav = workspace.Gravity * math.max(1 - (balloons * 0.05), 0.7)
                end
                if plr.Player and plr.Player:GetAttribute(ZC({73,115,79,119,108,84,97,114,103,101,116})) then
                    for _, owl in Z6:GetTagged(ZC({79,119,108})) do
                        if owl:GetAttribute(ZC({84,97,114,103,101,116})) == plr.Player.UserId and owl:GetAttribute(ZC({83,116,97,116,117,115})) == 2 then
                            playerGrav = 0
                            break
                        end
                    end
                end

                local rawLook = CFrame.new(offsetpos, plr[Z1._3].Position)
                local dist = (plr[Z1._3].Position - offsetpos).Magnitude
                local predicted = ZH(plr.Player, plr[Z1._3], projSpeed, grav, offsetpos)
                local newlook = ZI(rawLook, predicted, dist)

                if meta.projectile ~= ZC({111,119,108,95,112,114,111,106,101,99,116,105,108,101}) then
                    newlook = newlook * CFrame.new(
                        ZE.BowConstantsTable.RelX or 0,
                        ZE.BowConstantsTable.RelY or 0,
                        ZE.BowConstantsTable.RelZ or 0
                    )
                end

                local targetVel = meta.projectile == ZC({116,101,108,101,112,101,97,114,108}) and Vector3.zero or plr[Z1._3].Velocity

                local calc = ZA._S(newlook.p, projSpeed, grav, predicted, targetVel, playerGrav, plr.HipHeight, plr.Jumping and 50 or nil, rc)

                if calc then
                    local dir = (calc - newlook.p).Unit
                    local angle = math.acos(math.clamp(dir:Dot(Vector3.new(0, 1, 0)), -1, 1))
                    if angle > math.rad(1) and angle < math.rad(179) then
                        return {
                            initialVelocity = dir * projSpeed,
                            positionFrom = offsetpos,
                            deltaT = lifetime,
                            gravitationalAcceleration = grav,
                            drawDurationSeconds = 5
                        }
                    end
                end
            end
            return ZJ(...)
        end
        ZK = true
    end)
end
local function ZM()
    if not ZK or not ZE.ProjectileController then return false end
    return pcall(function()
        if ZJ then
            ZE.ProjectileController.calculateImportantLaunchValues = ZJ
            ZJ = nil
            ZK = false
        end
    end)
end
local ZN = {}
local function ZO(c) table.insert(ZN, c) end
local function ZP()
    for _, cn in ipairs(ZN) do
        pcall(function()
            if type(cn) == "thread" then task.cancel(cn)
            elseif cn.Disconnect then cn:Disconnect()
            elseif cn.destroy then cn:destroy() end
        end)
    end
    table.clear(ZN)
end
local ZQ = false
local function ZR()
    if ZQ then return end
    repeat task.wait() until game:IsLoaded()
    if not ZG() then warn(ZC({65,105,109,98,111,116,58,32,66,101,100,87,97,114,115,32,110,111,116,32,102,111,117,110,100,46})) return end
    Z9.start()
    ZO({Disconnect = function() Z9.stop() end})
    if Z1._1 then ZL() end
    if Z1._9 and Z1._A then
        local key = Enum.KeyCode[Z1._A]
        if key then
            local conn = Z5.InputBegan:Connect(function(input, gp)
                if gp then return end
                if input.KeyCode == key then
                    if ZK then ZM() else ZL() end
                end
            end)
            ZO(conn)
        else
            warn(ZC({73,110,118,97,108,105,100,32,116,111,103,103,108,101,32,107,101,121,58,32}) .. tostring(Z1._A))
        end
    end
    ZQ = true
    print(ZC({65,105,109,98,111,116,32,108,111,97,100,101,100,46,32,84,111,103,103,108,101,58,32}) .. (Z1._A or ZC({78,79,78,69})))
end
local oldInstances = getgenv().ProjectileAimbotInstances
if oldInstances then for _, f in pairs(oldInstances) do pcall(f) end end
getgenv().ProjectileAimbotInstances = {}
local function ZS()
    ZP()
    if ZM then ZM() end
    if Z9._H then Z9.stop() end
end
table.insert(getgenv().ProjectileAimbotInstances, ZS)
local scriptConn
scriptConn = game.DescendantRemoving:Connect(function(desc)
    if desc == script then
        ZS()
        if scriptConn then scriptConn:Disconnect() end
    end
end)
ZR()
